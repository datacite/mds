<?xml version="1.0" encoding="UTF-8"?>

<beans:beans xmlns="http://www.springframework.org/schema/security"
    xmlns:beans="http://www.springframework.org/schema/beans"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
                        http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security-3.0.xsd">

	<http auto-config="true" use-expressions="true">
        <form-login login-processing-url="/static/j_spring_security_check" login-page="/login" authentication-failure-url="/login?login_error=t"/>
        <logout logout-url="/static/j_spring_security_logout"/>
        
        <!-- API -->
        <intercept-url method="PUT" pattern="/v1/**" access="hasRole('ROLE_USER')" requires-channel="https" />
        <intercept-url method="POST" pattern="/v1/**" access="hasRole('ROLE_USER')" requires-channel="https" />
        <intercept-url method="DELETE" pattern="/v1/**" access="hasRole('ROLE_USER')" requires-channel="https"/>
        
        <!-- UI  -->
        <intercept-url pattern="/allocators/**" access="hasRole('ROLE_ADMIN')" requires-channel="https" />
        <intercept-url pattern="/datacentres/**" access="hasRole('ROLE_ALLOCATOR')" requires-channel="https" />        
        <intercept-url pattern="/datasets/**" access="hasRole('ROLE_DATACENTRE')" requires-channel="https" />
        <intercept-url pattern="/metadatas/**" access="hasRole('ROLE_DATACENTRE')" requires-channel="https" />
        <intercept-url pattern="/prefixes/**" access="hasRole('ROLE_ADMIN')" requires-channel="https" />
        <intercept-url pattern="/oaisources/**" access="hasRole('ROLE_ADMIN')" requires-channel="https" />
        
        <!-- static content -->
        <intercept-url pattern="/resources/**" access="permitAll" requires-channel="https" />
        <intercept-url pattern="/static/**" access="permitAll" requires-channel="https" />
        <intercept-url pattern="/**" access="permitAll" requires-channel="https" />
         
    </http>

    <authentication-manager alias="authenticationManager">
        <authentication-provider user-service-ref='myUserDetailsService'/>
    </authentication-manager>
    
    <beans:bean id="myUserDetailsService" class="org.datacite.mds.service.impl.UserDetailsServiceImpl" />

</beans:beans>