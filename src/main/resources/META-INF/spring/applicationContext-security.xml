<?xml version="1.0" encoding="UTF-8"?>

<beans:beans xmlns="http://www.springframework.org/schema/security"
    xmlns:beans="http://www.springframework.org/schema/beans"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
                        http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security-3.0.xsd">

	<http auto-config="true" use-expressions="true">
        <form-login login-processing-url="/resources/j_spring_security_check" login-page="/login" authentication-failure-url="/login?login_error=t"/>
        <logout logout-url="/resources/j_spring_security_logout"/>
                
        <!-- API -->
        <intercept-url method="GET" pattern="/datacentre/**" access="hasRole('ROLE_ALLOCATOR')" requires-channel="https" />
        <intercept-url method="PUT" pattern="/datacentre/**" access="hasRole('ROLE_ALLOCATOR')" requires-channel="https" />
        <intercept-url method="PUT" pattern="/doi/**" access="hasRole('ROLE_DATACENTRE')" requires-channel="https" />
        <intercept-url method="POST" pattern="/doi/**" access="hasRole('ROLE_DATACENTRE')" requires-channel="https" />
        <intercept-url method="GET" pattern="/metadata/**" access="hasRole('ROLE_DATACENTRE')" requires-channel="https" />
        <intercept-url method="PUT" pattern="/metadata/**" access="hasRole('ROLE_DATACENTRE')" requires-channel="https" />        
        <intercept-url method="DELETE" pattern="/metadata/**" access="hasRole('ROLE_DATACENTRE')" requires-channel="https"/>
        <intercept-url method="GET" pattern="/oaisource/**" access="hasRole('ROLE_ALLOCATOR')" requires-channel="https" />
        <intercept-url method="PUT" pattern="/oaisource/**" access="hasRole('ROLE_ALLOCATOR')" requires-channel="https" />
                
        <!-- UI -->
        <intercept-url pattern="/allocators/**" access="hasAnyRole('ROLE_ADMIN','ROLE_DEV')" requires-channel="https" />
        <intercept-url pattern="/datacentres/**" access="hasAnyRole('ROLE_ALLOCATOR','ROLE_DEV')" requires-channel="https" />        
        <intercept-url pattern="/datasets/**" access="hasAnyRole('ROLE_DATACENTRE','ROLE_DEV')" requires-channel="https" />
        <intercept-url pattern="/metadatas/**" access="hasAnyRole('ROLE_DATACENTRE','ROLE_DEV')" requires-channel="https" />
        <intercept-url pattern="/prefixes/**" access="hasAnyRole('ROLE_ADMIN','ROLE_DEV')" requires-channel="https" />
        <intercept-url pattern="/oaisourceses/**" access="hasAnyRole('ROLE_ADMIN','ROLE_DEV')" requires-channel="https" />
        <intercept-url pattern="/userinfo/**" access="isAuthenticated()" requires-channel="https" />
        
        <!-- static content -->
        <intercept-url pattern="/resources/j_spring_security_switch_user" access="hasRole('ROLE_DEV')" requires-channel="https" />
        <intercept-url pattern="/resources/**" access="permitAll" requires-channel="https" />
        <intercept-url pattern="/static/**" access="permitAll" requires-channel="https" />
        <intercept-url pattern="/**" access="permitAll" requires-channel="https" />
        
        <remember-me key="MDS"/>

    	<custom-filter ref="switchUserProcessingFilter" after="FILTER_SECURITY_INTERCEPTOR" />
    </http>

    <authentication-manager alias="authenticationManager">
        <authentication-provider user-service-ref='myUserDetailsService'/>
    </authentication-manager>
    
    <beans:bean id="myUserDetailsService" class="org.datacite.mds.service.impl.UserDetailsServiceImpl" />
    
    <beans:bean id="switchUserProcessingFilter" class="org.springframework.security.web.authentication.switchuser.SwitchUserFilter">
        <beans:property name="userDetailsService" ref="myUserDetailsService" />
        <beans:property name="switchUserUrl" value="/resources/j_spring_security_switch_user" />
        <beans:property name="exitUserUrl" value="/resources/j_spring_security_exit_user" />
        <beans:property name="targetUrl" value="/" />
    </beans:bean>
    

</beans:beans>